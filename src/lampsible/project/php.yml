---
- hosts: web_servers
  become: true
  gather_facts: true
  tasks:
    - include_role:
        name: "{{ item }}"
      loop:
        - apt
        - geerlingguy.php
        - composer
        - fail2ban
  vars_files:
    - vars/php/main.yml
