---
- name: Install Composer
  apt:
    name: composer
    state: present
  when: composer_packages != None and composer_packages | length > 0

- name: Create Composer project directory, if needed
  file:
    path: "{{ composer_working_directory }}"
    state: directory
    owner: www-data
    group: www-data
  when: composer_project != None and composer_project | length > 0

# This is because of idempotency issues in the Composer module.
# See https://github.com/ansible-collections/community.general/issues/725
- name: Check for composer.json
  stat:
    path: "{{ composer_working_directory }}/composer.json"
  register: composer_json

- name: Create Composer project
  community.general.composer:
    command: create-project
    arguments: "{{ composer_project }} {{ composer_working_directory }}"
    working_dir: "{{ composer_working_directory }}"
  environment:
    COMPOSER_ALLOW_SUPERUSER: "1"
  when: composer_project != None and composer_project | length > 0 and not composer_json.stat.exists

- name: Install Composer packages
  community.general.composer:
    command: require
    arguments: "{{ package }}"
    working_dir: "{{ composer_working_directory }}"
  loop: "{{ composer_packages }}"
  loop_control:
    loop_var: package
  environment:
    COMPOSER_ALLOW_SUPERUSER: "1"
  when: composer_packages != None and composer_packages | length > 0

- name: Make Apache owner of Composer working directory
  file:
    path: "{{ composer_working_directory }}"
    state: directory
    recurse: yes
    owner: www-data
    group: www-data
