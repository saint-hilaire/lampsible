---
- hosts: all
  become: true
  gather_facts: false
  roles:
    # TODO: feature/fix-apache
    - domain-for-wordpress
    # TODO: Maybe do this directly in ssl-certbot, not here.
    - apache-document-root
